import * as types from './modalsActions-types';

export const toggleStakingDialog = () => (
  {
    type: types.TOGGLE_STAKING_DIALOG
  }
);

export const toggleAboutDialog = () => (
  {
    type: types.TOGGLE_ABOUT_DIALOG
  }
);
